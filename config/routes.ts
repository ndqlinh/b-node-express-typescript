export const ROUTES = {
  register: '/api/auth/signup',
  signin: '/api/auth/signin',
  renew: '/api/auth/renew',
  auth: '/api/auth/{proxy+}',
  todos: '/api/todos',
  shopify: '/api/shopify/{proxy+}',
}
