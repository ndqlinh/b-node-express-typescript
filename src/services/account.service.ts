import { Account } from "../models/account.model";

export default class AccountService {
  constructor() {}

  register(account: any): Promise<Account> {
    return;
  }

  login(account: any): Promise<Account> {
    return;
  }
}
